//! Demonstrates a game with multiple 'regions' that are each separate game worlds.

//Main menu -> start game (displays region #) -> move to new region (displays region #) -> move back to starting
// region.

fn main() {}
